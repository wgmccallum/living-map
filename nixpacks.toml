[phases.setup]
nixPkgs = ["nodejs-20_x", "python3"]

[phases.install]
cmds = ["pip install .", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "uvicorn living_map.app:app --host 0.0.0.0 --port ${PORT:-8000}"
